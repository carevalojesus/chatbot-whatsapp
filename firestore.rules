rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Solo el backend (Admin SDK) accede a los datos.
    // Bloquea acceso directo desde apps cliente.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
