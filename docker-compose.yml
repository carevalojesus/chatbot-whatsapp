services:
  bot:
    build: ./bot
    container_name: chatbot-whatsapp-bot
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
